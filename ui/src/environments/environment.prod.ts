export const environment = {
  production: true
  callback: 'set after git ingore is setup',
  domain: 'set after git ingore is setupm',
  clientId: 'set after git ingore is setup',
  audience: 'set after git ingore is setup'
};
